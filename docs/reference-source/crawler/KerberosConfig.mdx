---
component: KerberosConfig
module: crawler/fs
locale: en
propertyCoverage: all
---

# KerberosConfig

## Summary

Kerberos/SPNEGO authentication settings for HDFS WebHDFS access.

## Notes

Used by `HdfsFetcher` when `authMethod` is set to `KERBEROS`.

At a minimum, provide a valid Kerberos configuration (`krb5.conf`) and one
of the following authentication mechanisms:

- keytab-based login (`keytabPath` + `principal`)
- ticket-cache login (`useTicketCache`)
- username/password JAAS login through HDFS fetcher credentials

When `loginModuleName` is not set, the crawler creates a built-in JAAS
configuration from the provided properties.

## Properties

### keytabPath

Path to the keytab file containing the principal key. When set,
authentication uses this keytab instead of requiring a password or
ticket cache.

### krb5ConfigPath

Path to the Kerberos configuration file (`krb5.conf`). When omitted,
the JVM default Kerberos configuration is used.

### loginModuleName

Optional JAAS login module entry name. When omitted, an internal JAAS
configuration is generated automatically.

### principal

Kerberos principal name (for example, `user@REALM`). Required for
keytab-based authentication.

### servicePrincipalName

Kerberos service principal name (SPN) of the target service, typically in the form `HTTP/hostname@REALM` or `HTTP/hostname`.

### useTicketCache

Whether the system ticket cache should be used. When enabled, an existing
ticket (for example from `kinit`) is used and no keytab or password is
required.

## Examples

<ReferenceExamples
  examples={[
    {
      title: "Kerberos with Keytab",
      description: "Uses a keytab and principal for non-interactive authentication.",
      codeByLanguage: {
        yaml: `class: HdfsFetcher
authMethod: KERBEROS
kerberosConfig:
  krb5ConfigPath: /etc/krb5.conf
  keytabPath: /etc/security/keytabs/hdfs-user.keytab
  principal: hdfs-user@EXAMPLE.COM`,
        json: `{
  "class": "HdfsFetcher",
  "authMethod": "KERBEROS",
  "kerberosConfig": {
    "krb5ConfigPath": "/etc/krb5.conf",
    "keytabPath": "/etc/security/keytabs/hdfs-user.keytab",
    "principal": "hdfs-user@EXAMPLE.COM"
  }
}`,
        xml: `<fetcher class="HdfsFetcher">
  <authMethod>KERBEROS</authMethod>
  <kerberosConfig>
    <krb5ConfigPath>/etc/krb5.conf</krb5ConfigPath>
    <keytabPath>/etc/security/keytabs/hdfs-user.keytab</keytabPath>
    <principal>hdfs-user@EXAMPLE.COM</principal>
  </kerberosConfig>
</fetcher>`,
      },
    },
    {
      title: "Kerberos with Ticket Cache",
      description: "Uses existing Kerberos tickets from the system cache.",
      codeByLanguage: {
        yaml: `class: HdfsFetcher
authMethod: KERBEROS
kerberosConfig:
  krb5ConfigPath: /etc/krb5.conf
  useTicketCache: true`,
        json: `{
  "class": "HdfsFetcher",
  "authMethod": "KERBEROS",
  "kerberosConfig": {
    "krb5ConfigPath": "/etc/krb5.conf",
    "useTicketCache": true
  }
}`,
        xml: `<fetcher class="HdfsFetcher">
  <authMethod>KERBEROS</authMethod>
  <kerberosConfig>
    <krb5ConfigPath>/etc/krb5.conf</krb5ConfigPath>
    <useTicketCache>true</useTicketCache>
  </kerberosConfig>
</fetcher>`,
      },
    },
  ]}
/>
