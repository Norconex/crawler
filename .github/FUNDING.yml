# Adds the "Sponsor" button to this repository, linking to the Norconex
# GitHub Sponsors listing. GitHub only recognizes this exact filename.
#
# Sponsorship is recognition, not a support contract -- see SPONSORS.md.

github: [Norconex]

# NOTE (2026-08-30): the repository Sponsor button does not appear despite
# this file being valid and on the default branch. GitHub's contents API
# lists it, but its funding parser returns no funding links
# (repository.fundingLinks is empty) and Settings offers to *create* the
# file. Verified correct: exact path and filename, minimal valid content,
# no duplicate funding file in .github/, root or docs/, repository public
# and not a fork, archived or template, Sponsorships feature enabled, and
# the organisation listing active and public with three published tiers.
# Committing the file through GitHub's own settings flow changed nothing
# either. Believed to be a GitHub-side issue; the organisation-level
# Sponsor button works, and SPONSORS.md and the README link to the
# sponsors page directly, so nothing is blocked on it.
